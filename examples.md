# Example issue bodies

```
    ```json request
    {
        "owner": "hashicorp-contrib",
        "repo": "setup-packer",
        "version": "latest"
    }
    ```
```

```
    ```json request
    {
        "owner": "hashicorp",
        "repo": "setup-terraform",
        "version": "latest"
    }
    ```
```

```
    ```json request
    {
        "owner": "aws-actions",
        "repo": "configure-aws-credentials",
        "version": "latest"
    }
    ```
```